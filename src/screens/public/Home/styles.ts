import { createStyles } from 'responsive-react-native'

export const styles = createStyles({
  container: {
    paddingTop: 16,
  },
  header: {
    marginBottom: 16,
  },
  content: {
    paddingBottom: 42,
    paddingRight: 24,
    paddingLeft: 24,
  },
  footer: {
    marginTop: 16,
  },
})
